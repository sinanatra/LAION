# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Generated by notebook/run_pipeline.py.
# images/ and metadata.json ARE checked in so the deployed site has real
# data — embeddings.npy (not used at runtime) and progress.json (local
# scan state) are not.
public/data/embeddings.npy
public/data/progress.json
